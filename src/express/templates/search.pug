extends snippets/layout.pug

block content
  main
    section.search-page
      div(class=`search-page__wrapper ${articles.length === 0 ? 'search-page__wrapper--empty' : ''}`)
        h1 Поиск
        .search.search-page__form
          form(action='#', method='get')
            label
              input(type='text', name='query', placeholder='Что ищем?', value=query)
            button.search__button.button.button--transparent(type='submit') Найти

          if articles.length > 0
            each article in articles
              .search__result
                time.search__date(datetime= article.createDate)= article.createDate
                a.search__link(href= `/articles/${article.id}`)= article.title
          else
            if query
              p.search-page__empty Ничего не нашлось

